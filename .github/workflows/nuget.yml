name: nuget

on:
  push:
    branches: [ main ]
  
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  nuget-pack:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore dependencies
        run: dotnet restore ManagedCode.Orleans.Identity.sln
      - name: Build
        run: dotnet build ManagedCode.Orleans.Identity.sln --configuration Release --no-restore
      #      - name: Test
      #        run: dotnet test --configuration Release
      - name: Pack
        run: dotnet pack ManagedCode.Orleans.Identity.sln -p:IncludeSymbols=false -p:SymbolPackageFormat=snupkg --configuration Release --no-build

      - name: publish nuget packages
        run: |
          shopt -s globstar
          for file in **/*.nupkg
          do
            dotnet nuget push "$file" --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
          done
