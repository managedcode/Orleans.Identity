<Project Sdk="Microsoft.NET.Sdk.Web">

    <PropertyGroup>
        <OutputType>Library</OutputType>
        <IsPackable>true</IsPackable>
    </PropertyGroup>

    <!--NuGet-->
    <PropertyGroup>
        <Title>ManagedCode.Orleans.Identity.Client</Title>
        <PackageId>ManagedCode.Orleans.Identity.Client</PackageId>
        <Description>Identity base on Orleans</Description>
        <PackageTags>managedcode, orleans, identity, session</PackageTags>
    </PropertyGroup>

    <ItemGroup>

        <PackageReference Include="ManagedCode.Communication" />
        <PackageReference Include="ManagedCode.Communication.Orleans" />
        
        <PackageReference Include="Microsoft.Orleans.Client" />
        <PackageReference Include="Microsoft.Orleans.Runtime" />

    </ItemGroup>

    <ItemGroup>
      <ProjectReference Include="..\ManagedCode.Orleans.Identity.Core\ManagedCode.Orleans.Identity.Core.csproj" />
    </ItemGroup>

</Project>
